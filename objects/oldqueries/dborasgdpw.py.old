class UserObject(dict):
    def __init__(s):
        object = {
            "type": "query",
            "id": "DBORASGDPW",
            "collections": ["DBORASGDPW"],
            "filterable": True,
            "request": "select timestamp, owner||' '||objtype||' '||object||' '||subobject label, sum(writes) value from DBORASGDPW group by timestamp, label order by timestamp"
        }
        super(UserObject, s).__init__(**object)
