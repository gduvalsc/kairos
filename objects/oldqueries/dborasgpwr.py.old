class UserObject(dict):
    def __init__(s):
        object = {
            "type": "query",
            "id": "DBORASGPWR",
            "collections": ["DBORASGPWR"],
            "filterable": True,
            "request": "select timestamp, owner||' '||objtype||' '||object||' '||subobject label, sum(writes) value from DBORASGPWR group by timestamp, label order by timestamp"
        }
        super(UserObject, s).__init__(**object)
